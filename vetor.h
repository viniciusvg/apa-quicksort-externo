/*Imprime os elementos de um vetor.*/
void imprime_vetor(int *, int);

/*Verifica se o vetor esta ordenado
    return  1 se estiver ordenado
            0 se estiver desordenado*/
int verifica_ordem(int *, int);

/*Copia valor original para vetor temporario.*/
void copia_vetor(int *, int *, int);

/*geracao de valores random*/
void gerar_numeros(int *, int);
